# require "bundler/gem_tasks"
# task :default => :spec
#the above code came with the bundle install

#the below code is from Avi's code along
require_relative './config/environment'
task :console do
  Pry.start
end
